// == it's equality / === means the same value and type
// the != means different / !== means different value and type

console.log(2 == '2');
console.log(2 === '2');

// saving it inside an variable
let variable = 2 == '2';
console.log(variable);

console.log('4' !== 4);